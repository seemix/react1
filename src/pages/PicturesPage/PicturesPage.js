import React from 'react';
import {Outlet} from "react-router-dom";

const PicturesPage = () => {
    return (
        <div>
            <Outlet/>
        </div>
    );
};

export default PicturesPage;